продукт: Dev-сервер
формат yaml-задания: v0.3

введение: |
  «Dev-сервер» предназначен для запуска сразу нескольких разных веб-сервисов в dev-версии.

  Сервер НЕ годится для запуска production-версии веб-сервиса из-за пониженных требований:

  - к бесперебойности работы
  - к сохранности пользовательских данных
  - к защищённости от утечки пользовательских данных

  «Dev-сервер» включает в себя серверные мощности (VPS), настроенную ОС на базе Debian, веб-сервер Nginx и Certbot.
  Конфиги веб-сервера, специфичные для отдельно взятого веб-сервиса остаются за рамками продукта «Dev-сервер».

польза: # 😀 Зачем нужен ещё один продукт?
  P1. Программист бэкендер - Запустить dev-версию веб-сервиса:
    старые способы решить проблему:
      - Взять по отдельной VPS на каждый новый веб-сервис: [@b]
      - Каждый веб-сервис разместить отдельно в PaaS: [@b]
    фичи:
      - a. Уложиться в стоимость одного самого дешёвого VPS когда запущены несколько веб-сервисов
      - b. Установить защищённое сетевое соединение с пользователями (SSL)
      - c. Запустить докеризированное ПО
      - d. Запустить недокеризированный веб-сервис
      - e. Чувствовать себя в безопасности после развёртывания веб-сервиса в dev-версии
      - f. @unusable Ограничить доступ другим программистам с правом доступа к общему серверу

риски:
  R1. Бан от certbot:
    - Общий reverse-proxy сервер запущен в docker-контейнере
    - Из-за ошибки конфигурации новый сертификат генерируется при каждом перезапуске контейнера
    - Случились пять перезапусков веб-сервера в один день
    - certbot отказался выдавать SSL-сертификат и выдал бан на несколько дней
    - @P1b Пару дней веб-сервисы на сервере доступны только в небезопасном режиме браузера

  R2. Устарел SSL сертификат:
    - @P1b Веб-сервис доступен только в небезопасном режиме браузера пока не вмешается сис.админ

  R3. Снаружи взломан доступ к докер-серверу:
    - Боты нашли сервер с открытым доступом к докер-серверу
    - @P1e Боты запустили на сервере майнинг или ферму ботов
    - @P1e Боты остановили запущенные ранее контейнеры и потёрли данные

  R4. Снифферинг коммуникации с докер-сервером:
    - Сниффер трафика нашёл токены для доступа к внешним сервисам
    - @P1e Злоумышленник добрался до учёток к платным сервисам
    - @P1e Злоумышленник добрался до БД

истории:
  Программист бэкендер:  # Умеет CLI, Systemd и Nginx. Ориентируемся на учебную программу Девмана
    - A1. @P1c Запустить веб-сервис с помощью Docker Compose:
      - "[] a. Приступил к развёртыванию веб-сервиса → Вижу список запущенных контейнеров"
    - A2. @P1 Запустить два сайта:
      - "[] a. Приступил к развёртыванию второго сайта → Вижу страницы двух запущенных сайтов, каждый защищён HTTPS, висит на стандартном порту и на своём поддомене"

как проверить:
  - "[ ] T1. @R1 Веб-сервер установлен на уровне хоста, без докера"
  - "[ ] T2. @R2 В журнале systemd указана следующая дата обновления SSL-сертификата"
  - "[ ] T3. @R2 Systemd перезапускает веб-сервер через reload после обновления сертификата Certbot"
  - "[ ] T4. @R3 @R4 Докер-клиент подключается к серверу по ssh"
